# mt-aging

## Data retrieval
The dataset is downloaded from UK Biobank and it is validated by `ukbmd5`, followed by decryption with `ukbunpack`. Then, `dconvert` is used to convert encrypted dataset into R format with field explanations.


